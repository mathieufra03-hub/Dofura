# Historique des chantiers — Dofura

> Archive des chantiers fermés et des leçons de l'ancienne méthode de travail,
> extraite de CLAUDE.md le 2026-08-03. Conservée pour consultation ponctuelle —
> ce n'est plus la référence courante, voir CLAUDE.md pour l'état actuel du projet.

## Chantiers fermés (extrait de "Chantiers en cours / problèmes ouverts")

2. **Grand nettoyage du repo — FERMÉ (2026-07-08).** ~35 scripts jetables supprimés, outils réutilisables déplacés dans `scripts/`, `dofura_sorts_complet.json` supprimé (orphelin), `dofura.db` sorti du suivi Git (JSON sources restés en Git, voir règle 9 et piège #10), `src/` mort et `frontend/frontend/` résiduel supprimés, `VITE_API_URL` en place (code + Vercel), `changer_api.py`/`app_dump.txt` supprimés, README rédigé. Backup complet (local + Drive de Popo) fait avant toute suppression.
3. **Effets spéciaux sorts — FERMÉ (2026-07-08).** `dofura_effets_speciaux.json` complété (1577 résolutions diceNum→nom, 20 introuvables car contenu retiré du jeu) et branché dans `formater_effet()` : les effets `description: '#1'`/vide résolvent désormais le vrai nom du sort au lieu d'être masqués. Marqueurs `{{~ps}}`/`{{~zs}}` nettoyés avec vraie pluralisation (`{{~ps}}` → "s" si la valeur ou le max de la plage ≠ 1, filet de sécurité si non numérique). Testé sur Bêlement (non affecté, inchangé), Tornade (singulier "1 case") et Rage (plage "6 à 10 Dommages").
4. **Chantier #4 — Filtres zone/famille sur l'encyclopédie des monstres — FERMÉ (2026-07-09).** Enquête initiale : `famille` propre pour 4 930/4 932 monstres (32 valeurs, dont "Créatures de quête" = 56% du total à elle seule) ; `zones` (table séparée, un monstre a 0 à N zones) propre pour 1 590/4 932 (372 valeurs distinctes) — les 3 342 sans zone sont presque tous des créatures de quête/invoquées, cohérent. Découverte importante : aucune page de liste/grille de monstres n'existait avant ce chantier (juste un menu de recherche typeahead) — construite dans la foulée. Backend : `/monstres` accepte désormais `famille`/`zone`/`page`/`page_size` (garde `search`), réponse passée de liste brute à `{total, page, page_size, monstres}` (Hero, la recherche existante, adaptée en conséquence), valeur spéciale `__aucune__` pour "sans famille"/"sans zone" ; nouvel endpoint `/monstres/filtres` pour peupler les menus. Frontend : nouveau composant `MonstresPage` (grille paginée 48/page, recherche, menu famille natif, combobox zone maison avec recherche intégrée pour les 372 valeurs, bouton Réinitialiser, état vide explicite), styles regroupés dans un objet dédié pour préparer la refonte graphique à venir. Tuile EncycloGrid et lien Navbar "Monstres" branchés vers la vraie page (ils ne faisaient que revenir à l'accueil jusqu'ici). Testé en local (frontend+backend, navigateur) : grille complète, zone seule, famille+zone combinés, clic→détail, recherche 0 résultat + reset, sans-famille (2 monstres, image/famille manquantes affichées sans planter), recherche Hero toujours fonctionnelle. Limite connue non bloquante : le retour depuis une fiche détail ramène à l'accueil plutôt qu'à la grille filtrée (état des filtres non conservé) — amélioration possible plus tard, pas un bug.
4bis. **Filtres famille/zone en cascade — FERMÉ (2026-07-09).** Retour utilisateur sur le chantier #4 : les menus famille/zone étaient indépendants, un joueur pouvait choisir une combinaison sans aucun monstre (page vide sans explication). `/monstres/filtres` calcule désormais chaque liste à partir de l'AUTRE filtre uniquement (familles filtrées par la zone active, zones filtrées par la famille active — jamais par soi-même, sinon la valeur choisie disparaîtrait de son propre menu), plus `sans_famille`/`sans_zone` pour n'afficher ces options que si un monstre y correspond vraiment. **Bug trouvé et corrigé en testant :** les fetchs `/monstres` et `/monstres/filtres` n'ignoraient pas les réponses périmées — un changement de famille suivi d'un autre juste après (avant la fin du premier fetch) pouvait afficher des résultats ne correspondant plus au filtre affiché (ex. sélectionner "Créatures d'Amakna" affichait parfois "Créatures Archimonstres" en résultat). Corrigé par un identifiant de requête incrémental sur les deux effets frontend, qui ignore toute réponse qui n'est plus la dernière demandée — un piège de concurrence à surveiller pour tout futur fetch dépendant d'un filtre qui change vite. Testé dans les deux sens (famille→zones, zone→familles), combo compatible conservée, Réinitialiser restaure les listes complètes.
5. **URL dofura.fr** : à acheter/configurer.
7. **Chantier #5 — Lisibilité bonus/malus des effets — FERMÉ (2026-07-08).** Signalement initial : Bêlement affichait "2 PM" sans signe, soupçonné d'être un malus (devrait être "-2 PM"). Enquête sur `api.dofusdb.fr/effects/{id}` : chaque effet a `characteristicOperator` ("+"/"-"), `boost`, `bonusType` et un `oppositeId` — des champs qu'on ne stockait pas dans `dofura_effects.json` (seuls `description`/`is_in_percent` gardés jusqu'ici). **Bêlement utilise l'effet 128, confirmé en jeu par Popo : +2/+3 PM, un vrai bonus, pas un malus** — le signalement initial était une fausse alerte. Le vrai besoin qui en ressort : Ankama écrit le "-" en dur dans le texte des malus mais jamais de "+" pour les bonus → ambiguïté de lisibilité. Corrigé : `dofura_effects.json` enrichi (`characteristic_operator`/`boost`/`bonus_type`/`opposite_id`, script `scripts/enrichir_effets.py` supprimé après usage, backup pris avant modif) ; `formater_effet()` (main.py) ajoute un "+" explicite devant les bonus dont le texte démarre par la valeur brute, et expose un champ `polarite` ("bonus"/"malus"/`None`) prêt pour une future coloration vert/rouge côté frontend. Testé sur Bêlement (+2/+3 PM), Ralentissement Araknéen (-2 PA, inchangé), Ralenti (-2 PM, inchangé), vol de PA (inchangé, pas de + erroné). **Audit complémentaire (règle 13) :** échantillon de 30 sorts + 15 monstres comparés champ par champ à l'API DofusDB en direct → **0 écart trouvé**, toutes les valeurs stockées (stats, résistances, coûts de sorts, effets) correspondent à la source. Deux points mineurs notés en passant (non bloquants) : 14 sorts sur 8019 ont une erreur de scraping (IDs placeholder négatifs, 404 sur l'API) ; les champs `max_cast_per_target`/`max_global_cast_per_turn`/`min_cast_interval`/`initial_cooldown`/`cast_in_line`/`cast_in_diagonal` renvoyés par `/sorts/{id}` dans main.py ne sont jamais alimentés par `scripts/scraper_sorts.py` (toujours `null`) — à nettoyer un jour, sans urgence.

8. **Audit complet d'affichage des effets + 4 corrections — FERMÉ (2026-07-09).** Script `scripts/audit_affichage_effets.py` (gardé, pas jetable — réutilisable pour revalider après tout changement de `formater_effet()` ou des JSON d'effets) : fait passer les 34 159 effets de tous les sorts par une copie de `formater_effet()` et classe tout texte anormal par type. Corrigé (un commit par correction) : **double espace** résiduel après retrait des balises `<sprite>` (514→0) ; **balise `<sprite>` brute** dans les noms résolus via `EFFETS_SPECIAUX_DATA` — cette branche spéciale ne passait jamais par le nettoyage sprite (56→0, concentré sur Huppermage) ; **16 marqueurs `{{spell,ID,N::Label}}`** restés bruts dans `dofura_effets_speciaux.json`, échappés à l'extraction du chantier #3 (80→0 effets touchés) ; **effets masqués invisibles par Ankama** — `/sorts/{id}` exclut désormais tout effet dont `visibleInTooltip`/`visibleInBuffUi`/`visibleInFightLog` sont tous `false` (3 519 effets sur 34 159 concernés, dont les 575 qui causaient une partie du placeholder `#3` non résolu). **Comportement confirmé et assumé, ne plus se reposer la question :** le texte masqué (`None`) pour un `diceNum` non résolu dans `dofura_effets_speciaux.json` (contenu retiré du jeu) est un choix voulu, pas un bug — rien à corriger, c'est fini. Reste ouvert : voir chantier #6 ci-dessous, désormais FERMÉ.
9. **Chantier #6 — Résolution des IDs d'état (placeholder `#3`) — FERMÉ (2026-07-09).** Sur l'exemple concret demandé (Fourniture, Soryo Givrefoux) : `#3` s'est révélé être un nombre brut (champ `value`), pas un ID — bonne surprise qui a évité de généraliser une hypothèse fausse. En creusant les 21 templates utilisant `#3`, **3 familles distinctes confirmées par tests API réels** : (1) `EFFECTS_ETAT_VALEUR` `{950,951,952}` ("État #3"/"Enlève l'état #3"/"Désactive l'état #3", 1 788 instances) — le champ `value` est un ID d'état résolu via `api.dofusdb.fr/spell-states/{id}` (endpoint trouvé par exploration, pas deviné) → `dofura_etats_speciaux.json` (564 IDs, 0 introuvable) ; (2) `EFFECTS_ETAT_DICE` `{788}` ("Châtiment de #2 #1 sur #3 tours") — `diceNum`/`diceSide` aussi des IDs d'état, même résolution ; (3) `EFFECTS_SORT_CONDITION` (15 templates `"#1 : +#3 X"`, 329 instances) — `diceNum` est l'ID d'un sort déjà dans `dofura_sorts.json`, **aucun appel API nécessaire**. **Piège réel évité par vérification directe :** certains IDs (298, 2105) existent à la fois comme sort ET comme spell-state avec des noms sans rapport (298 = sort "Totem du Feu" mais état "Djim") — utiliser la mauvaise collection aurait produit un texte faux avec confiance. Un 2e lot de 74 marqueurs `{{spell,ID,N::Label}}` résiduels trouvé et corrigé dans `dofura_etats_speciaux.json` lui-même (même bug que le chantier #5, cette fois dans le nouveau fichier). Testé sur Fourniture, Flèche Douloureuse ("État Affaibli"), Châtiment Sanguin ("Châtiment de Kwavent Magnétor sur 5"), Épée du destin du Dopeul (nom de sort résolu). **Audit final : placeholder `#3` 2 743→0.** Reste 25 textes masqués (`None`) sur 30 640 effets : 13 déjà connus (contenu retiré du jeu) + 12 nouveaux légitimes (sorts référencés absents de `dofura_sorts.json`, qui n'est pas un catalogue exhaustif — construit à partir des seuls sorts utilisés par des monstres) — amélioration nette, ces 12 affichaient avant un ID brut cassé. **Domaine "affichage des effets sur les monstres" considéré clos.**

10. **Chantier #7 — Objets/Équipements (Phase 1 roadmap) — FERMÉ (2026-07-09).** Session nocturne autonome (scraping) puis session normale (dédoublonnage + intégration).
    **Scraping (nuit) :** 4 endpoints DofusDB identifiés et testés : `/items` (21 738), `/recipes` (4 858, `ingredientIds`+`quantities`+`jobId`), `/item-sets` (928 panoplies, bonus progressifs par nombre de pièces), `/item-types` (239 catégories). Pagination en liste ($limit/$skip, plafond 50/page) au lieu de requêtes individuelles. Bug trouvé et corrigé pendant la nuit : la fonction de log elle-même plantait tout le script sur une erreur `Permission denied` transitoire, corrigé en la rendant increvable (retry puis abandon silencieux de cette seule ligne). Reprise sur incident validée en conditions réelles (crash à skip=3000, redémarrage reparti pile de là, 0 perte). Résultat : 4 collections à 100%, 0 erreur, 0 doublon.
    **Dédoublonnage :** `dofura_recipes.json` (244 Mo) et `dofura_item_sets.json` (55,7 Mo) intégraient l'objet complet de chaque item référencé au lieu d'un simple ID (la même ressource utilisée dans des centaines de recettes était dupliquée intégralement à chaque fois) ; `dofura_items.json` (211 Mo) embarquait aussi sa panoplie et son type en entier en plus de sa propre description multilingue. `scripts/nettoyer_items.py` (gardé, réutilisable) : ne garde que le français, les IDs de référence, retire les métadonnées techniques → items 211→15,7 Mo, recipes 233→2 Mo, item-sets 53→1,4 Mo, tous sous la limite GitHub de 100 Mo. Effets renommés `from`/`to`→`diceNum`/`diceSide` pour réutiliser `formater_effet()`.
    **Audit avant endpoint (`scripts/audit_affichage_items.py`, demande explicite Popo) :** a révélé 18 066 effets sur 47 547 (38%) avec un `effectId` totalement absent de `dofura_effects.json` (jamais construit qu'à partir des sorts de monstres) — 164 IDs distincts récupérés via `api.dofusdb.fr/effects/{id}`, 0 introuvable.
    **Bug majeur trouvé en testant l'endpoint (au-delà de ce que l'audit textuel pouvait détecter — le texte produit était syntaxiquement valide, juste numériquement faux) :** les objets n'ont que 2 emplacements numériques (`from`/`to`) contre 3 pour sorts/monstres (`diceNum`/`diceSide`/`value`). Pour la famille de templates `"#1 : +#3 X"` (bonus lié à un sort connu, ex. "Fracture : +2 Portée maximale"), le champ `effects` (déjà simplifié par Ankama) ne porte pas le 3e nombre — il n'existe que dans `possibleEffects`, jugé à tort pur doublon et supprimé lors du dédoublonnage. Résultat avant correction : tous les objets de cette famille affichaient "+0" au lieu du vrai bonus. Corrigé : `nettoyer_items.py` reconstruit un index `(effectId, diceNum)→value` depuis `possibleEffects` (confirmé : `value` vaut 0 partout pour les stats classiques Force/Dommages/etc., donc aucune régression sur la simplification initiale — le trou ne concernait que cette famille précise). `formater_effet()` généralisé : la détection "sort-condition" passe d'une liste figée de 15 `effectId` (issue du seul chantier #6 monstres/sorts) à une détection par motif structurel (`^#1\s*:`) sur le template — 5 `effectId` supplémentaires découverts en testant les objets (ex. 289 "#1 : ligne de vue désactivée", sans `#3`) auraient été ratés par la liste figée. Nouveau filet de sécurité général : tout texte avec un `#` résiduel est masqué plutôt qu'affiché cassé. Correction annexe : numérotation des paliers de panoplie décalée d'un cran (palier stocké = nombre réel de pièces équipées, pas +1).
    **Endpoints de base :** `GET /objets` (liste, recherche par nom), `GET /objets/{id}` (détail : effets formatés, recette avec noms d'ingrédients résolus, panoplie avec bonus par palier). Nouvelles tables `init_db.py` : `objets`, `objets_effets`, `recettes`, `panoplies`, `panoplies_effets` — 0,73s pour régénérer toute la db, aucun souci de démarrage Railway malgré ~470 Mo de sources désormais en Git.
    **Testé** (avant/après le fix `value`) : Épée de Boisaille (44, témoin non affecté), Casque Keutumedi (8619, "+0"→"+2/+3/+1" corrigé), Panoplie du Bouftou (2411, palier 1 = 1 pièce confirmé), Flèche Douloureuse/Châtiment Sanguin/Bêlement/Ralenti (non-régression sorts/monstres). **Audit final : 0 anomalie restante hors "texte masqué" assumé** (3 665 sur 47 547 effets objets, mêmes causes déjà acceptées côté monstres : templates vides sans valeur ou IDs non résolvables statiquement).
    Fichiers bruts (`dofura_items_brut.json` etc., ~500 Mo cumulés) gardés localement en sécurité, jamais commités.
11. **Chantier #8 — Pages Équipements et Ressources — FERMÉ (2026-07-09).** État des lieux avant code : `super_type_nom` a survécu au dédoublonnage du chantier #7 (26 valeurs, 0 objet sans catégorie). Vérification contenu par contenu (pas de suppositions) pour trancher le périmètre "équipement portable" : Familier = vrais familiers équipables (Volkorne/Dragodinde/Montilier), mais Suiveur ("Personnage suiveur"), Compagnon ("Signe de X") et Équipement de percepteur (Tunique/Fers/Bannière de Percepteur) sont des mécaniques annexes, pas un slot d'équipement joueur — exclus après validation Popo. Périmètre final : Arme/Amulette/Anneau/Bottes/Ceinture/Chapeau/Cape/Bouclier/Dofus-Trophée-Prysmaradite/Familier (3 826 objets) pour `/equipements`, `Ressource` seul (3 639) pour `/ressources`. Les 3 072 "Certificat" enquêtés pour un chantier futur Élevage se sont révélés être des objets de quête invisibles sans rapport — la vraie catégorie montures est "Certificat de monture" (267, Dragodinde/Volkorne/Muldo), corrigée avant d'être notée en chantier futur.
    Backend : `/objets` généralisé avec `categorie` (mappée vers une liste fixe de `super_type_nom`, pas de saisie libre), `type`, `tranche_niveau` (1-50/51-100/101-150/151-200), `search`, pagination — même format `{total,page,page_size,objets}` que `/monstres`. Nouvel endpoint `/objets/filtres?categorie=...` : pas de cascade à double sens nécessaire ici (contrairement à famille/zone des monstres) puisque la catégorie est fixée par la page et ne dépend jamais du type choisi.
    Frontend : composant unique `ObjetsPage` paramétré par `categorie` (DRY, aucun copier-coller de page) réutilisé pour les deux pages — seuls titre/placeholder/messages changent, injectés depuis le point d'appel plutôt que devinés par grammaire à l'intérieur du composant. **Lacune découverte en écrivant le composant, pas anticipée au plan initial :** contrairement aux monstres (fiche détail déjà existante avant le chantier #4), aucune fiche détail objet n'existait côté frontend malgré l'endpoint `/objets/{id}` prêt depuis le chantier #7 — question posée à Popo plutôt que décidée seule, résolue par l'ajout d'`ObjetDetailPage` (effets colorés bonus/malus, recette, panoplie avec navigation croisée entre pièces). Navigation : `navLinks`/`EncycloGrid` mis à jour ("Objets"→"Équipements" + nouvelle entrée "Ressources", comptes réels), `App()` passe d'un état booléen `browsing` à trois destinations distinctes (`selectedMonstre`/`selectedObjet`/`browsing`).
    **Bug réel trouvé en testant dans le navigateur** (l'audit textuel du chantier #7 ne l'avait pas détecté — le texte produit était syntaxiquement valide, juste faux) : contrairement aux sorts/monstres où `diceNum` est toujours une magnitude positive (le signe ne vit que dans le texte du template), **les objets encodent le signe directement dans le nombre brut** (confirmé sur 953 effets bonus + 68 malus : jamais de `from` négatif côté bonus, jamais positif côté malus). Un template malus déjà préfixé `"-#1..."` recevait donc un `diceNum` déjà négatif → `"--100 Force"` au lieu de `"-100 Force"` (repro : Abranneau). Corrigé en neutralisant le signe (`abs()`) dans `formater_effet_objet()` avant l'appel au formateur partagé — `formater_effet()` lui-même non touché, aucune régression sorts/monstres. Vérifié au passage : `"-11 à -40 Force"` (plage malus, les deux bornes signées) est le rendu correct du template Ankama, pas un bug.
    **Testé** : Abrarc (témoin complet), Abranneau (malus corrigé), La Baguette des Limbes (plage malus confirmée correcte), navigation croisée entre membres de panoplie, Ressources (recherche, tranche 101-150 → 737, reset), cas 0 résultat, navigation directe Ressources→Équipements via navbar (filtres réinitialisés au changement de catégorie).
12. **Chantier #8bis — Sort accordé par les objets + flag légendaire — FERMÉ (2026-07-09).** Signalement : l'Ardeur d'Oto Mustam (chapeau légendaire niv. 200) a en jeu un sort intégré, absent de notre fiche. Diagnostic avant code (règle 13) : `effectId 1175` (template `"#1"`, même famille que `EFFETS_SPECIAUX_DATA`) présent dans `possibleEffects` mais **jamais dupliqué dans `effects`** par Ankama — `diceNum` est l'ID du sort accordé, résolu via `api.dofusdb.fr/spells/{id}` (154/155 résolus, 1 introuvable = contenu retiré du jeu, même situation déjà acceptée aux chantiers #3/#6). **Motif vérifié au-delà du signalement initial (règle appliquée, voir leçon 4 ci-dessus) : ne touche pas QUE les légendaires.** 25/25 légendaires concernés, mais aussi 319 objets non-légendaires (344 au total, toutes catégories) — Dofus, armes, capes, mais aussi des catégories hors périmètre actuel (Équipement de percepteur, Objet de quête...). En creusant plus large, **d'autres `effectId`** sont absents de `effects` par le même mécanisme (`983` "Échangeable : #1", `10` "Attitude #3", `148` "Quelqu'un vous suit !"...) mais ce sont des marqueurs internes/cosmétiques, pas des effets de jeu — volontairement PAS mergés (auraient été une régression, pas une correction). Panoplies vérifiées : aucune ne porte ce trou (seuls `10`/`148` y apparaissent, déjà écartés).
    `scripts/nettoyer_items.py` complété : `EFFECTS_A_RECUPERER_DE_POSSIBLE = {1175}` fusionne l'entrée manquante depuis `possibleEffects` (uniquement si visible et absente d'`effects`, liste volontairement restreinte à ce seul `effectId` confirmé) ; nouveau champ `legendaire` (depuis `isLegendary`, seul champ de rareté dans les données — vérifié, pas d'autre "epic"/"mythique"). `dofura_effects.json` complété (effet 1175) et `dofura_effets_speciaux.json` étendu (154 sorts accordés) — même mécanisme de résolution que le chantier #3, pas un nouveau système. `init_db.py` : colonne `legendaire` sur `objets`. Frontend : badge "★ Légendaire" sur `ObjetDetailPage` (doré, affiché uniquement si `data.legendaire`).
    **Testé avant commit** : Ardeur d'Oto Mustam (badge + "Ardeur de la Cité Sombre" dans les effets), Dofus Pourpre non-légendaire ("Pourpre Profond" affiché, badge absent — bonne différenciation), Épée Langue de Feu (catégorie Arme, "Langue de Feu" + "Lié au personnage" affichés, badge absent). Trois catégories distinctes couvertes.
13. **Chantier #8ter — Détail des sorts accordés par les objets — FERMÉ (2026-07-09).** Suite du chantier #8bis : le nom du sort accordé était affiché en simple ligne dans les effets de l'objet, sans son contenu réel. Diagnostic avant code (règle 13) sur `api.dofusdb.fr/spells/{id}` : deux familles de contenu selon le sort — texte `description` statique (117/186, ex. "Ardeur de la Cité Sombre") ou, à défaut, effets structurés du grade précis indiqué par l'objet (`spellLevels`, 69/186, même schéma diceNum/diceSide/value/effectId que les sorts de monstres du chantier #6, réutilisable tel quel par `formater_effet()`) — jamais aucun des deux dans 0 cas, donc toujours quelque chose à montrer. **Piège trouvé avant d'écrire le script (pas en le débuggant après coup) :** un même sort peut être accordé à un grade différent selon l'objet (16/154 cas vérifiés, ex. sort 25293 aux grades 1/2/3 selon l'item) — une clé de stockage par seul `spell_id` (comme `dofura_sorts.json`) aurait mélangé les contenus ; vérifié que réutiliser `dofura_sorts.json` était donc factuellement impossible (pas juste indésirable) avant de créer `dofura_sorts_objets.json`, clé composite `"{spell_id}_{grade}"` (186 couples résolus, 1 introuvable = même sort mort déjà connu du chantier #8bis).
    Backend : `SORTS_OBJETS_DATA` chargé au démarrage comme les autres fichiers de résolution ; `detail_objet()` isole désormais la ligne `effect_id 1175` des effets génériques de l'objet (sinon le nom du sort apparaissait deux fois) et construit un champ `sort_accorde` dédié (`nom`/`description`/`effects`, ces derniers filtrés par `effet_visible()` puis formatés par le `formater_effet()` partagé — pas `formater_effet_objet()`, les effets de sorts n'ont pas le problème de signe propre aux objets). Frontend : nouvelle section "SORT ACCORDÉ" sur `ObjetDetailPage`, entre la fiche et les effets propres de l'objet, sur le modèle visuel des autres blocs (nom en évidence, puis description en texte ou effets en liste à puces selon ce que les données offrent).
    **Testé** : Ardeur d'Oto Mustam (description complète affichée), Épée Langue de Feu (sort "Langue de Feu" : ni description ni effet visible côté Ankama — comportement légitime déjà accepté au chantier #6, la section montre juste le nom sans rien inventer). `scripts/resoudre_sorts_objets.py` supprimé après usage (règle 6).
14. **Chantier Donjons (encyclopédie, Phase 1 roadmap) — FERMÉ (2026-07-09).** Vision : fiches concises ("10 secondes"), anti gros-pavé.
    **Frontière données/éditorial vérifiée avant code (règle 13) :** `api.dofusdb.fr/dungeons` (187 donjons) donne nom/niveaux/difficulté/zone (`subarea`→`area`)/boss/monstres/objets requis pour entrer/dispo recherche-groupe-hall-trousseau — **aucun champ image**, et la liste de monstres est **plate** (aucune API testée ne donne un découpage par salle). Les IDs de boss/monstres correspondent déjà exactement à notre table `monstres` (vérifié : ID 147 = Bouftou Royal des deux côtés) → **zéro re-scraping de monstres nécessaire**, jointure directe. `drops` (table existante, sourcée Dofensive) liée par nom (pas d'ID objet) → jointure `drops.nom = objets.nom` pour le tableau de drops du donjon, avec repli propre (nom seul, sans image) si aucune correspondance — pas garanti à 100%, assumé. Mécaniques de boss / points d'attention / composition conseillée / découpage par salle : **rien de tout ça dans aucune API**, 100% éditorial, jamais scrapé ni inventé.
    **Piège de persistance évité avant d'écrire le schéma :** `init_db.py` DROP+recrée toutes les tables à chaque démarrage depuis les JSON sources (règle 9) — un contenu éditorial stocké uniquement en colonnes SQLite aurait donc été perdu au premier redéploiement Railway. Le guide de boss (`mecaniques`/`salles`/`compo_conseillee`, 3 champs validés avec Popo) vit donc dans `dofura_donjons_guides.json` (clé `donjon_id`, `{}` vide pour l'instant), chargé en mémoire par `main.py` comme `EFFETS_SPECIAUX_DATA`/`SORTS_OBJETS_DATA` — jamais dans une table SQLite qui serait rasée au prochain déploiement. Prêt pour un futur mini-chantier Krag qui n'aura qu'à remplir le JSON, sans toucher au schéma. Section "Guide du boss" masquée côté frontend tant que les 3 champs sont vides (pas de "à compléter" qui ferait croire à du contenu).
    Backend : `scripts/scraper_donjons.py` (réutilisable comme `scraper_sorts.py`/`scraper_items.py`, PAS supprimé) → `dofura_donjons.json`. Tables `init_db.py` : `donjons` (avec `boss_principal_id`, premier boss ou premier monstre si pas de boss), `donjons_monstres` (junction avec flag `est_boss`), `donjons_objets_requis` (junction). Endpoints `/donjons` (liste+recherche+filtre zone+pagination façon `/monstres`), `/donjons/filtres`, `/donjons/{id}` (détail complet : boss principal, roster de monstres, objets requis résolus, tableau de drops agrégé, guide). **Liens croisés** (exploite la base relationnelle propre, argument central de la Vision) : `detail_monstre()` expose désormais `donjons` ("Apparaît dans"), `detail_objet()` expose `donjons_requis` ("Nécessaire pour") — coût marginal quasi nul grâce aux tables junction déjà là pour l'usage principal.
    Frontend : `DonjonsPage` (grille paginée, recherche, filtre zone) + `DonjonDetailPage` (bandeau clés, accès/clé requise, guide si rempli, monstres du donjon avec boss mis en évidence doré, tableau de drops), Navbar/EncycloGrid branchés (`Donjons` pointait nulle part avant, comme `Équipements`/`Ressources` avant le chantier #8). Sections "Apparaît dans"/"Nécessaire pour" ajoutées sur `MonstrePage`/`ObjetDetailPage`.
    **2 donjons "Expédition" sans zone/boss/monstres/difficulté** (`Expédition - Cour du Bouftou Royal`, `Expédition - Donjon des Squelettes`) : contenu incomplet côté DofusDB (probablement un mode non représenté comme un vrai donjon explorable dans cette collection), gardés tels quels avec repli "—" plutôt qu'exclus sur intuition (leçon 1) — testé, aucun plantage.
    **Testé en navigateur** : grille (recherche, filtre zone, pagination), fiche Cour du Bouftou Royal complète (bandeau, clé requise cliquable, roster de monstres avec boss doré, 23 drops avec jointure objet réussie), guide masqué (JSON vide), lien croisé boss→fiche monstre→"Apparaît dans"→retour donjon, lien croisé clé→fiche objet→"Nécessaire pour", cas Expédition sans zone (pas de plantage).
15. **Chantier Panoplies (encyclopédie, Phase 1 roadmap) — FERMÉ (2026-07-10).** Les 928 panoplies et leurs bonus par palier étaient déjà en base depuis le chantier #7 (tables `panoplies`/`panoplies_effets`, déjà affichées sur les fiches objets) — chantier de mise en autonomie, pas de nouvelle donnée à scraper.
    **Inspection avant plan (règle 13) :** aucun `/panoplies` ni `/panoplies/{id}` n'existait, `objets/{id}` construisait déjà le bloc panoplie mais rien d'accessible de façon autonome. Aucun champ image propre à la panoplie côté DofusDB (`item-sets`, vérifié brut et nettoyé) — résolu en stockant `image_objet_id` (1er `item_id` du set) au nettoyage, rejoint à `objets` pour l'icône de tuile. Niveau propre à la panoplie confirmé fiable (`level` du set lui-même, pas dérivé des membres — vérifié Bouftou = 20, cohérent sur ses 8 pièces).
    **2 anomalies trouvées en creusant, toutes deux validées avec Popo avant traitement (règle 14) :** (1) 403 panoplies sur 928 (43%) sans aucun bonus stocké — coïncide presque exactement avec `isCosmetic: true` côté DofusDB (407 sets, 4 seulement avec un vrai effet), champ supprimé par erreur d'omission au dédoublonnage du chantier #7. Choix retenu : champ `cosmetique` réintégré + filtre dédié sur la page (même logique que `legendaire` au #8bis), rien n'est masqué. (2) 1 panoplie orpheline (id 703 "Panoplie d'apparat de Stroud") : doublon exact (même nom/niveau/item_ids) de l'id 732, qui est la vraie référencée par les 2 objets concernés — anomalie de la source DofusDB elle-même, pas du pipeline. Exclue explicitement (`SETS_A_EXCLURE` dans `nettoyer_items.py`, motif documenté en commentaire) plutôt qu'affichée avec une fiche vide.
    Backend : `GET /panoplies` (recherche, filtre type bonus/cosmétique, filtre tranche de niveau façon `/objets`, pagination), `/panoplies/filtres`, `/panoplies/{id}` (membres, bonus par palier via `formater_effet_objet()` déjà existant, aucune nouvelle logique de formatage). Colonne `image_objet_id` sur `panoplies`, résolue par jointure comme `donjons.boss_principal_id`→`monstres`.
    Frontend : `PanopliesPage` (grille paginée, recherche, filtres) + `PanoplieDetailPage` (bandeau avec badge cosmétique éventuel, objets membres cliquables, bonus par palier ou message "sans bonus statistique"), tuile EncycloGrid + lien navbar (Panoplies ne pointait nulle part avant, comme Donjons/Équipements avant leurs chantiers respectifs). Nom de la panoplie sur `ObjetDetailPage` rendu cliquable → complète la réciproque objet↔panoplie déjà entamée au chantier #7.
    **Testé en navigateur** : grille (927, recherche "Bouftou" → 2, filtre Cosmétiques seul → 406, filtre niveau 51-100 seul, combinaison des deux → 101, Réinitialiser), fiche Abranoplie (bonus par palier corrects, membres cliquables), fiche Ankamanoplie (badge Cosmétique, message sans bonus), navigation croisée objet→panoplie→objet dans les deux sens.
16. **Chantier Zones (encyclopédie, dernier domaine de la roadmap Phase 1) — FERMÉ (2026-07-10).** Annuaire à deux niveaux (régions → sous-zones), comme le découpage réel du jeu — pas la version plate initialement envisagée.
    **Découverte centrale avant plan (règle 13) :** deux notions de "zone" coexistaient déjà en base sans lien entre elles — `zones` (table monstre↔zone, 372 valeurs, granularité fine type "Antre du Dragon Cochon", sourcée Duffus, simple texte) et `donjons.zone` (42 valeurs, granularité large type "Astrub", résolue par `scraper_donjons.py` via `subarea→area` DofusDB). Recoupement exact par nom : 6/372 seulement. Plutôt que choisir entre les deux (perdre les monstres ou perdre les donjons), vérification de faisabilité d'une reconstruction de la hiérarchie manquante côté DofusDB (même mécanisme `subarea→area` que celui déjà utilisé pour les donjons) : **371/372 noms de sous-zone (99,7%) correspondent à un nom de subarea DofusDB réel**, et les 46 régions obtenues recoupent **exactement (100%)** les 42 zones déjà présentes dans `donjons.zone` — même collection source, même nommage. Décision : annuaire à deux niveaux plutôt qu'un choix à somme nulle entre deux moitiés de la donnée.
    **2 sous-zones orphelines, non résolues par choix et non par oubli :** "Territoire des Dragodindes Sauvages" (aucune subarea DofusDB de ce nom) et "Cimetière" (nom partagé par 2 subareas d'aires différentes — Amakna et Incarnam, 19 monstres concernés — ambiguïté réelle, aucun choix arbitraire entre les deux fait). Restent accessibles via une recherche globale sous-zones sur la page Régions (sinon perdues de la navigation) plutôt qu'exclues.
    **Tranche de niveau (directive Popo) :** min/max dérivé du grade le plus bas de chaque monstre (son niveau "naturel" de rencontre, pas la plage complète de ses grades qui inclut ses évolutions les plus fortes — testé sur Bouftou id 101, grades 22-30, cohérent), affiché tel quel même quand l'écart est large (ex. "Cryptes du cimetière" = Niv. 1-440, une zone qui mélange réellement des monstres très différents en puissance) — aucun seuil de masquage arbitraire, fidèle à la donnée réelle.
    Données : nouveau script `scripts/scraper_zones.py` (réutilisable comme `scraper_donjons.py`, reprend son mécanisme `subarea→area`) → `dofura_zones_areas.json` (372 sous-zones → région ou `null`), ajouté à la liste des sources JSON Git-trackées (règle 9).
    Backend : nouvelle table `zones_areas` (nom sous-zone → région). `GET /zones` (46 régions, recherche, compteurs sous-zones/donjons, image = 1er monstre alpha), `GET /zones/{region}` (donjons de la région, sous-zones avec niveau), `GET /sous-zones?search=` (recherche plate incluant les orphelines), `GET /sous-zones/{nom}` (monstres, niveau min/max, région parente si connue). Piège de routage évité avant d'écrire les routes : 8 noms de sous-zone sont identiques à leur propre région (ex. "Dimension Obscure") — vérifié avant coup, d'où deux ressources distinctes (`/zones/{region}` et `/sous-zones/{nom}`) plutôt qu'un seul chemin ambigu.
    Frontend : `RegionsPage` (grille 46, recherche + résultats sous-zones directs), `RegionDetailPage` (donjons + sous-zones cliquables), `SousZoneDetailPage` (monstres, niveau, lien région ou badge "Région inconnue" pour les orphelines). Tuile EncycloGrid "Zones" branchée (comptait "800+" en dur, jamais cliquable — corrigé à 372, le vrai total).
    **Testé en navigateur** : grille régions (46, compteurs sous-zones/donjons corrects), fiche Amakna (45 sous-zones, 14 donjons, "Cryptes du cimetière" affiche bien Niv. 1-440 sans masquage), fiche sous-zone (monstres, niveau, région cliquable), recherche globale "Cimeti" (remonte l'orpheline "Cimetière" sans région à côté des 6 autres avec leur région), fiche de l'orpheline (badge "Région inconnue", monstres corrects), navigation sous-zone→monstre (fiche existante, non régressée).

17. **Chantier Quêtes (liste + fiche, encyclopédie) — FERMÉ (2026-07-13).** Demandé directement par Popo hors de la liste des candidats notée en Reprise (compagnon farm/craft, Krag, REFONTE.md — reportés, toujours d'actualité pour la prochaine session).
    **Inspection avant plan (règle 13) :** `api.dofusdb.fr/quests` (1 976 quêtes) embarque déjà tout (étapes, objectifs, récompenses, objets complets) en une seule pagination bulk — aucun appel `/quest-objectives` séparé nécessaire. **Découverte qui a changé le plan initial :** la catégorie "Quête de Dofus" imaginée dans la maquette (Principale/Secondaire/Répétable/Dofus) ne correspond à aucun champ réel — `categoryId` (43 valeurs) est un découpage thématique/zone façon journal de quêtes en jeu, pas un axe principale/secondaire, et en creusant les objets Dofus réels (`type_nom == "Dofus"`, 34 items) via `questsThatReward`/`achievementsThatReward` d'Ankama : **seuls 3 Dofus sur 34 sont donnés directement par une quête** (Dofawa, Dotruche, Dofus Argenté Scintillant) — tous les Dofus emblématiques (Émeraude, Turquoise, Ivoire...) sont donnés par un **succès**, domaine pas encore commencé. Décision validée avec Popo : catégorie "Quête de Dofus" abandonnée pour ce chantier (2 catégories réelles : Répétable si `repeatType != 0`, sinon Autre) — sera réintroduite proprement au futur chantier Succès/Chasse aux Dofus plutôt que bricolée ici sur une donnée bancale.
    **2e découverte, changeant aussi le plan initial :** XP/Kamas ne sont stockés qu'en **ratios** (`experienceRatio`/`kamasRatio`), jamais en montant absolu — le vrai chiffre dépend d'une formule serveur Ankama non documentée. Afficher un nombre comme le fait la maquette aurait été une invention (règle 13). Décision validée avec Popo : indicateur qualitatif seulement ("+ XP"/"+ Kamas", sans chiffre), les objets/titres restant eux des ID exacts donc affichés tels quels.
    **Zone résolue comme Donjons/Zones (chaîne subarea→area)**, avec une limite réelle : pas d'endpoint `/maps` public chez DofusDB (collection vide, testé) — la zone est donc résolue uniquement depuis les objets `map` déjà embarqués dans les objectifs des quêtes, via un **index global mapId→subArea construit sur les 1 976 quêtes** (une carte visitée par une quête peut résoudre le point de départ d'une autre qui ne l'a pas dans ses propres objectifs) : fait passer la couverture de 76% à 94,5% (109/1 976 quêtes restent sans zone, essentiellement les quêtes "avis de recherche" sans lieu de départ fixe — gap assumé, même logique que les donjons Expédition/zones orphelines).
    **Bug de données trouvé en testant dans le navigateur** (pas détectable par simple lecture du JSON, repéré au premier affichage réel d'une fiche) : les descriptions d'étapes contenaient des marqueurs bruts non résolus — `{{npc,ID[,X,Y]::Label}}`, `{{monster,...::Label}}`, `{{transition,...::Label}}`, `{{ui,...::Label}}` (même famille que `{{spell,ID,N::Label}}` des chantiers #3/#5/#8, 3 nouveaux préfixes), plus `<sprite name="X">` et `<shortcut=.../>` hors accolades. Catalogue exhaustif vérifié sur les 4 450 textes avant nettoyage (aucun `{{}}` sans `::`, seulement 2 familles de balises) : nettoyé à la source dans `scraper_quetes.py`, 0 résidu après coup.
    **Cross-linking :** `need.generated.dungeons` des objectifs de chaque étape donne un lien donjon direct et fiable (335/344 quêtes `isDungeonQuest` couvertes, les 9 restantes n'ont simplement aucun objectif du type "combat en donjon"). "Succès parent" **structurellement absent** de la fiche (pas de table `succès` en base — sera ajouté quand ce domaine existera, pas avant) : conforme à la spec ("seulement s'ils existent dans les relations de la base"). Réciproque ajoutée côté fiche donjon ("Quêtes associées"), coût marginal quasi nul comme au chantier Donjons.
    **Lien externe DofusPourLesNoobs :** deviner une URL de fiche à partir du nom testé et confirmé cassé (404) — la page de recherche interne du site (`?s=nom+encodé`) répond en revanche 200 systématiquement, même sur un nom bidon. Bouton "Guide complet →" toujours vers cette recherche, jamais vers une URL construite à l'aveugle, `target="_blank"` + `rel="noopener noreferrer"`.
    Backend : 6 tables (`quetes`, `quetes_etapes`, `quetes_etapes_items`, `quetes_prerequis_quetes`, `quetes_prerequis_objets`, `quetes_donjons`) ; endpoints `/quetes` (recherche, catégorie/zone en multi-sélection façon `/monstres`, tri zone/niveau/A→Z), `/quetes/filtres`, `/quetes/{id}`. **Aucune nouvelle table de progression créée** : `progression_joueur`/`favoris` (Phase 4, génériques `element_type`/`element_id`) réutilisées telles quelles (`quete_etape`/`quete`) — exactement l'usage prévu par leur conception.
    Frontend : `QuetesPage` (lignes avec ★, tri, panneau de filtres façon Bestiaire/Objets) + `QuetePage` (étapes cochables avec `.df-check`/`.df-progress`, classes CSS déjà préparées dans `tokens.css` en anticipation de ce chantier et jamais utilisées avant — premier consommateur). Bug trouvé en testant et corrigé avant de proposer le commit (règle du workflow validé) : le message "Connecte-toi pour sauvegarder..." s'affichait même en étant connecté (oubli de condition `!token`).
    **Testé en navigateur, connecté (PopoTest) et déconnecté** : liste (recherche, tri, filtre Répétable → 608 résultats, filtre zone, favoris), fiche complète (case cochée → persiste après rechargement complet de la page), navigation croisée quête↔donjon dans les deux sens, message de connexion requis en déconnecté (favori et étape), lien DPLN correctement construit et sécurisé.

18. **Sous-chantier Quêtes — position précise + résumés Lorn (zone Astrub) — FERMÉ (2026-07-14).** Suite directe du chantier 17, sur demande explicite de Popo : enrichir la fiche quête avec une position exacte façon jeu et un résumé condensé, zone par zone.
    **Refus tenu malgré insistance (le seul point notable de ce sous-chantier) :** Popo a demandé à plusieurs reprises de scraper les guides de quêtes de DofusPourLesNoobs et de les faire "assimiler" par Lorn avant résumé. Refusé à chaque relance — lire un contenu éditorial protégé pour en produire un résumé reste une œuvre dérivée à l'échelle de ~2000 quêtes, exactement ce que la règle Donjons (piège DPLN/Papycha) interdit. Alternative retenue et validée : Lorn résume uniquement le texte officiel Ankama des étapes déjà scrapé (`dofura_quetes.json`), jamais une page DPLN. Piste alternative explorée à la demande de Popo (wiki Fandom Dofus) : écartée par Popo lui-même car trop ancien (probablement resté sur Dofus 2.x).
    **Position exacte `[x,y]` comme en jeu :** trouvée disponible sans scraping supplémentaire — le même objet `map` déjà embarqué dans les objectifs de quête (utilisé pour la zone) porte aussi `posX`/`posY` (repère monde identique à celui affiché en jeu) et un `name` de lieu précis (ex. "Boutique de Kerubim") quand Ankama en donne un. Étendu à l'index global `mapId->subArea` déjà construit au chantier 17 (deux nouveaux index `mapId->nom` et `mapId->xy`) : 1 651/1 976 quêtes (83,5%) ont désormais des coordonnées exactes.
    **Limite vérifiée avant de promettre quoi que ce soit (règle 13) :** un marqueur cliquable précis dans le décor (façon capture DPLN) n'existe dans aucun type d'objectif de l'API (catalogue exhaustif des `typeId`/`className` vérifié : aller voir un PNJ, combattre, ramener un objet, découvrir une carte, artisanat, aucun n'a de position pixel/cellule). Deviner cette position aurait été une invention — non fait. Seule l'image de la carte (hébergée par DofusDB, pas DPLN) + le repère `[x,y]` du monde sont exposés ; la "feuille de route" objectif par objectif et la carte cliquable restent hors scope (reportées, la 2e recoupant le chantier futur #7 "Carte interactive").
    **Contenu Lorn (118 résumés + points clés pour les 5 quêtes multi-étapes)** dans `dofura_quetes_guides.json`, même logique de fichier séparé que `dofura_donjons_guides.json` — masqué côté fiche tant qu'une zone n'a pas été traitée. Correspondance 118/118 vérifiée par script avant intégration (aucun ID manquant ni en trop).
    **Testé en navigateur** : "Le maître des clefs" (position + résumé + points clés + reste de la fiche), "Shushu et Lulu" (résumé seul, sans points clés), quête hors Astrub (`guide: null`, section entièrement absente, pas de "à compléter" trompeur).
    **Reste ouvert pour une prochaine session** (à trancher avec Popo avant de continuer, règle 1) : étendre les résumés Lorn zone par zone, au-delà d'Astrub.

19. **Sous-chantier Quêtes — étapes en actions GPS + ressources + bouton `/travel` — FERMÉ (2026-07-14).** Demande directe de Popo : le texte narratif Ankama des étapes ("Vous devez continuer à montrer votre détermination : allez promener votre carcasse dans le donjon des Squelettes") jugé pas assez concret — voulu à la place : icône + verbe + cible en gras + position, **généré automatiquement, jamais rédigé à la main**.
    **Découverte qui a rendu ça possible sans inventer :** chaque étape Ankama porte en réalité plusieurs "objectifs" bruts sous-jacents (aller voir un PNJ, combattre, ramener un objet, découvrir une carte...), chacun typé (`className`) et paramétré (PNJ/monstre/objet/quantité en ID direct, pas seulement en texte gabarit) — jamais exploités avant cette passe. Résolution par ID depuis nos propres bases déjà en place (`dofura_items.json`, `dofura_monstres.json`, `dofura_donjons.json`, PNJ étendus à tous les objectifs et plus seulement le point de départ).
    **Règles de transformation (toutes vérifiées sur des cas réels avant généralisation, pas supposées) :**
    - Étape référençant un donjon (`need.generated.dungeons`) → réduite à une seule action "Traverser le {donjon}" (combat + sortie + retour PNJ tous implicites).
    - "Aller voir un PNJ" retiré dès qu'un autre type d'objectif existe dans la même étape (la position de la vraie action suffit à se repérer) ; gardé seulement s'il est le SEUL objectif (le but de l'étape est alors littéralement d'aller parler à quelqu'un).
    - Objectifs consécutifs strictement identiques (doublons bruts constatés côté Ankama) dédupliqués.
    - Repli pour les objectifs sans cible structurable (rares, type "récolte en texte libre") : texte officiel de l'objectif lui-même (déjà court), jamais une phrase inventée.
    **Position par action (pas seulement au lancement de la quête), validée sur un vrai cas de nuance** : pour "Le maître des clefs", le PNJ à trouver à l'étape 1 vit au **Lac de Cania** `[-6,-37]`, différent du lieu de lancement de la quête (Taverne d'Astrub) — confirme que la position par objectif capture une donnée réellement plus précise que celle déjà affichée en en-tête, pas une redite.
    **Bouton `/travel`** (2e demande de Popo, même sous-chantier) : chaque position `[x,y]` devient un bouton qui copie `/travel x,y` dans le presse-papier (commande d'autopilote du jeu) via `navigator.clipboard.writeText`, retour visuel "Copié !" 1,5s. Non vérifiable par clic automatisé dans l'environnement de test de Claude Code (le navigateur automatisé n'a pas le focus système, l'API Presse-papier le refuse) — limite de l'outil de test, pas du code, à valider par Popo en cliquant réellement.
    **Ressources à prévoir** (3e ajout) : liste agrégée de tous les objets "Ramener à un PNJ" sur la quête entière (pas seulement le prérequis de départ qui débloque la quête), quantités cumulées si un objet revient à plusieurs étapes — section séparée de "Prérequis", dérivée des mêmes objectifs déjà résolus.
    **Astuce dialogue : structure prête, jamais remplie automatiquement.** Popo a demandé à savoir quoi répondre aux PNJ — vérifié qu'aucun endpoint DofusDB ne résout le contenu des dialogues (aucune route `/dialogs`, `/dialog-messages`, etc. ne répond). Champ optionnel `astuce_dialogue` ajouté dans `dofura_quetes_guides.json` (même mécanisme que `resume`/`points_cles`), mais seulement rempli au compte-goutte quand Popo identifie lui-même un vrai piège de dialogue en jouant — jamais généré, jamais deviné.
    **Aération de la fiche (retour visuel après premier rendu) :** étapes jugées trop tassées — padding vertical augmenté (22px), sous-actions d'une même étape séparées par un filet pointillé, positions en gros/gras via le bouton `/travel` lui-même.
    Backend : 2 nouvelles tables (`quetes_etapes_actions`, `quetes_ressources`), `/quetes/{id}` enrichi (`etapes[].actions`, `ressources`), le champ `description` de l'étape reste en base (utile en repli/référence) mais n'est plus le mode d'affichage principal.
    **Testé en navigateur** : "Le maître des clefs" (4 actions exactes, dont la nuance de position Lac de Cania), "Shushu et Lulu" (13 objectifs bruts réduits à 6 actions concises + liste de ressources à 3 objets), quête hors Astrub (génération automatique confirmée, aucune régression).

20. **Chantier Grimoire — fusion Équipements/Ressources/Bestiaire/Panoplies en une page unique "Grimoire des Secrets" — FERMÉ (2026-07-29).** Demandé directement par Popo : recherche globale groupée par type (onglet "Tout" par défaut) + onglets Équipements/Ressources/Monstres/Panoplies, résultat cliqué → panneau latéral par-dessus la liste (jamais de nouvelle page), filtres spécialisés adaptés au type sélectionné. Le mot "métiers" retiré partout de l'interface, remplacé par "ressources".
    **Fait en deux temps, pause validée après le premier (règle 1) :** phase 1 = structure (recherche, onglets, résultats groupés, panneau latéral), phase 2 = filtres spécialisés — Popo a validé la phase 1 ("vasy continue cest tres bien") avant l'attaque de la phase 2.
    **Zéro nouvel endpoint nécessaire** : réutilise tels quels `/objets` (+`/objets/filtres`), `/monstres` (+`/monstres/filtres`), `/panoplies` (+`/panoplies/filtres`) — les mêmes que les anciennes pages dédiées (`ObjetsPage`, `BestiairePage`, `PanopliesPage`), juste appelés depuis un nouveau composant unique `GrimoirePage`. Un seul point de construction des paramètres par type (`grimoireParams()`), pas de duplication (règle DRY explicitement demandée).
    **Mode "Tout" :** 4 aperçus en parallèle (comptage + 6 résultats par type), "Voir tout →" bascule vers l'onglet dédié en pagination complète (48/page, comme avant). Seul filtre commun aux 4 types : le niveau (plage min/max, bornes = union des 3 types qui en ont une) — **les panoplies ne sont pas affectées par ce filtre en mode "Tout"** (leur backend n'accepte qu'une tranche catégorielle `tranche_niveau`, pas une plage continue), limite assumée et documentée dans le code.
    **Panneau latéral :** réutilise tel quel les fiches détail existantes (`ObjetDetailPage`, `MonstrePage`, `PanoplieDetailPage`) dans un cadre flottant (pile de navigation interne pour les liens croisés du type panoplie → objet membre → ..., "← Retour" dépile un niveau, "✕" ferme tout d'un coup et restaure exactement l'état de recherche). Les liens croisés vers un donjon (hors périmètre du Grimoire) font sortir du panneau et ouvrent la fiche donjon en pleine page comme avant.
    **Bug trouvé et corrigé en testant (phase 1) :** le bouton "✕" de fermeture défilait avec le contenu du panneau au lieu de rester ancré à l'écran — `position: fixed` au lieu de `absolute`.
    **Anciennes pages (`ObjetsPage`, `BestiairePage`, `PanopliesPage`) gardées dans le code mais plus jamais appelées** (retirées de la navbar et de la grille Encyclopédie) — suppression prévue au prochain nettoyage, volontairement pas faite tout de suite pour garder une référence pendant la phase 2.
    **Testé en navigateur** : recherche "bouftou" (comptage exact par groupe), clic résultat → panneau, drill panoplie → objet membre → "← Retour" revient à la panoplie, "✕" ferme et restaure l'état ; filtres testés un par un (type équipement "Ceinture", légendaire uniquement, région Amakna → cascade sous-zone, panoplie cosmétique) — tous fonctionnels avec chips + "Tout effacer". Redimensionnement mobile non vérifié visuellement (outil de resize de la session sans effet réel sur le viewport), mais le CSS (`min(860px, 100vw)` pour le panneau, classes `.df-filters-panel`/`.df-filters-toggle` déjà éprouvées ailleurs) suit le même mécanisme que les pages déjà responsive du site.

21. **Migration des taux de Songes vers un modèle base × multiplicateur (10 intensités) — FERMÉ (2026-08-03).** L'ancien `dofura_songes_taux.json` codait en dur les seuls relevés de Paradoxe I (36 lignes, releves ponctuels). Popo a fourni une v2.0 : une table de taux de BASE par catégorie + un multiplicateur par intensité, d'où les 10 intensités (Rêve I-III, Paradoxe I-IV, Cauchemar I-III) se recalculent. **Formule validée : `taux_affiché = taux_base × multiplicateur_butin`, exacte au millième sur 28 relevés en jeu** — `verifier_taux.py` (racine du projet) l'atteste, à relancer après toute future modification des taux (non-régression, 28/28 attendu).
    **Audit avant tout code (règle 1) :** comparaison précise ancien vs nouveau sur le seul point de recoupement possible (Paradoxe I) — légendes/cosmétiques cohérents (petits écarts, arrondis), mais **les runes astrales divergent systématiquement selon un ratio qui varie par PALIER, pas par rune ni par intensité** (palier II ×1,1, III ×1,3, IV ×1,2, V ×1,4, palier I inchangé). Décision Popo : le nouveau fichier fait autorité sur toute la ligne (l'ancien = relevés ponctuels non reproductibles), mais ce ratio n'est pas du bruit — documenté dans `dofura_songes_taux.json` (`_meta.reserve_connue`) : **les taux de runes actuels sont un PLANCHER**, un multiplicateur de palier supplémentaire est probable mais non implémenté, en attente d'une mesure en jeu. Ne pas l'ajouter aux calculs sans cette mesure.
    **`taux_songes.py` à la racine du projet, pas dans `scripts/`** (déplacé en cours de chantier) : ce n'est pas un script jetable, `init_db.py` en dépend pour démarrer (`from taux_songes import migrer`) — un `sys.path.insert` pointant vers `scripts/` aurait dépendu du répertoire courant, fragile en production où `init_db.py` est lancé par `main.py` via `subprocess` dans un cwd non garanti.
    **Bug trouvé en migrant (corrigé) :** Diplôme de Feur générait des lignes à Rêve I-III alors que sa condition en jeu ("Niveau d'intensité du Songe > 3") l'interdit — la restriction n'existait que dans un champ texte (`condition_jeu`), pas sous forme de drapeau exploitable. Ajouté `"intensite_rang_min": 4` au JSON, la fonction de migration ignore désormais tout item cosmétique dont le rang de l'intensité traitée est inférieur à son `intensite_rang_min`. Total passé de 256 à 247 lignes (10 par intensité Rêve, au lieu de 13).
    **10 intensités désormais toutes couvertes — ne plus jamais dire qu'il manque 45 combinaisons intensité×catégorie** (c'était vrai avant ce chantier, obsolète depuis).
    **2 bugs supplémentaires trouvés en testant les endpoints** (silencieux : aucune erreur levée, juste des données fausses ou manquantes affichées) :
    - `dofura_songes_items.json` : `diplome_feur` et `rune_astrale_legendaire` avaient `intensite_min: null` au lieu de `"paradoxe"` (contrairement aux 30 autres items du fichier) — polluait l'estimation agrégée "n'importe quel item de la catégorie" à Rêve (`categorie=cosmetique` renvoyait `disponible: false` alors que les 6 Bouclirêve avaient de vraies données, un seul item sans donnée suffisant à faire échouer tout le calcul groupé). Corrigé en alignant ces 2 items sur les autres.
    - `main.py`, boucle `RUNES_HORS_TRACKER` de `/songes/taux` : la requête de disponibilité des paliers ne filtrait pas par intensité/niveau, affichant des runes fantômes (taux `null` partout) à Rêve. Corrigée pour réutiliser `charger_taux()` (déjà filtré).
    **Bug trouvé lors de la validation visuelle de Popo (corrigé) :** `songe_taux.taux` stockait la valeur déjà arrondie à 3 décimales — deux intensités différentes pouvaient tomber sur le même arrondi (ex. Paradoxe II à 0,0028 et III à 0,0032 arrondissaient toutes deux à 0,003), rendant leurs estimations "1 tous les X songes" identiques à tort, sur l'agrégat de catégorie **et** sur chaque item individuel (même cause partagée). **Règle retenue : on arrondit à l'AFFICHAGE, jamais au STOCKAGE** — un `round()` en base détruit la précision de façon irréversible pour tout calcul en aval. Corrigé : `taux_songes.py` stocke désormais le produit exact, `formaterTaux()` (déjà présent côté frontend) continue de arrondir à 3 décimales pour l'affichage, sans aucun changement visuel sur les pourcentages montrés.
    **Bandeau prospection** (page Les Taux) : rendu deux fois quand la catégorie Cosmétiques est dépliée (un exemplaire toujours visible + un second dupliqué dans le détail déplié) — gardé un seul exemplaire, toujours visible. Texte corrigé : "la prospection du groupe" était factuellement faux — **la prospection qui influe sur les cosmétiques est individuelle, par personnage**, pas une valeur de groupe.
    **Donnée retenue pour référence :** un cosmétique précis (Bouclirêve Étoile) est ≈25 à 30× plus rare qu'une légende "n'importe laquelle parmi les 26" à intensité égale (≈190 songes contre ≈7, Cauchemar III, 4 joueurs) — comparaison entre un item unique et un pool de 26, pas deux taux de base bruts équivalents.
    **Testé à chaque étape :** `verifier_taux.py` 28/28 tout du long, script de jointure `cle_taux` (`songe_items_trackables` ↔ `songe_taux`, 0 orphelin des deux côtés), les 10 intensités vérifiées via `/songes/taux` (aucun `null`/fantôme là où il ne devrait pas y en avoir), validation visuelle finale de Popo sur la page Les Taux en local.

22. **Diagnostic et correction des tables manquantes en prod + endpoint `/admin/refresh-encyclopedie` — FERMÉ (2026-08-03).** Trouvé en validant le déploiement du chantier 21 : `/songes/taux` renvoyait 500 en prod (`sqlite3.OperationalError: no such table: songe_items_trackables`), alors que tout fonctionnait en local.
    **Cause : un déploiement Railway synchronise le CODE, jamais le SCHÉMA de la base** (piège #12, nouvellement documenté) — `base_deja_peuplee()` (`main.py`) ne relance `init_db.py` que si `monstres` est vide, donc toute table ajoutée au schéma après la toute première mise en ligne de prod n'est jamais créée, même aux redéploiements suivants.
    **Diagnostic fait en lecture seule sur un backup de prod téléchargé** (jamais sur la prod elle-même) : `scripts/diag_tables_prod.py` compare le schéma attendu (extrait du texte source d'`init_db.py`) à ce qui existe réellement dans un fichier `.db` donné. **10 tables manquantes trouvées** : 3 encyclopédiques du Suivi de Songes (`songe_items_trackables`, `songe_taux`, `songe_boss_modifs`) et **les 7 tables de progression joueur du tracker en entier** (`songe_personnages`, `songe_teams`, `songe_team_membres`, `songe_runs`, `songe_run_participants`, `songe_drops`, `songe_journal`) — aucune n'avait jamais existé en prod, donc aucun joueur n'avait jamais pu utiliser le tracker de Songes en ligne jusqu'ici.
    **Nouvel endpoint `POST /admin/refresh-encyclopedie`** (token `X-Admin-Token` en en-tête, comparaison à temps constant, backup automatique avant toute écriture — annulé si le backup échoue, opération loguée) : `CREATE TABLE IF NOT EXISTS` autorisé sur les 10 tables du registre (encyclopédiques et progression joueur — créer une table absente ne détruit rien), remplissage depuis les JSON UNIQUEMENT sur les 3 encyclopédiques, JAMAIS sur les 7 de progression joueur même fraîchement créées vides. Garde vérifiée avant CHAQUE écriture, portant sur l'opération (create/fill) et pas seulement le nom de table, plus une vérification statique du SQL exécuté (aucun DROP/DELETE/UPDATE possible). **Registre volontairement limité à ces 10 tables** (pas un mécanisme générique pour toute table future) — à mettre à jour à la main si le schéma gagne encore une table plus tard dans le même cas.
    **Ne jamais importer `init_db.py` depuis `main.py`** pour construire cet endpoint : son code de haut niveau (chargement JSON + connexion à `DB_PATH` + DROP/CREATE/INSERT) s'exécuterait dès l'import, sur la vraie base — whitelists et SQL de création copiés indépendamment dans `main.py`, à tenir synchronisés à la main avec `init_db.py`.
    **Bug trouvé en testant (corrigé) :** la regex de garde `\b(DROP|DELETE|UPDATE)\b` faisait un faux positif sur `ON DELETE CASCADE` (clause de contrainte FK légitime, présente sur 3 des tables de progression joueur), provoquant un 500 qui a quand même laissé une table créée malgré l'erreur. **A révélé que `CREATE TABLE` n'est pas annulé par un `conn.rollback()` sous sqlite3/Python** (piège #13, nouvellement documenté) — corrigé à deux niveaux : regex affinée (exclusion de `ON DELETE`/`ON UPDATE`) et validation complète de tout le registre déplacée AVANT le backup et toute connexion en écriture, pour qu'un bug de ce genre ne puisse plus jamais causer une écriture partielle.
    **Testé sur une copie de la base locale reproduisant exactement l'état de prod** (les 10 tables supprimées) : 1er appel → 10 créées, 3 remplies, 7 vides ; lignes de progression joueur de test insérées, 2e appel → lignes strictement identiques avant/après (le scénario réel du prochain appel en prod, une fois de vraies données joueur présentes) ; cas d'échec (token absent, mauvais token, tentative de remplissage sur une table protégée) tous refusés. Déployé et validé directement en prod par Popo (jamais appelé depuis Claude Code) : Les Taux, L'Œil de Draconiros, création de team, drop simulé — tout confirmé fonctionnel de bout en bout.
    **Reste ouvert, non urgent :** chaque appel de l'endpoint laisse un fichier `dofura.db.avant-refresh-TIMESTAMP.db` (~12 Mo) sur le volume Railway, aucun ménage automatique — un seul fichier pour l'instant, à surveiller si l'endpoint est rappelé souvent.

## 📜 Leçons historiques (ancienne méthode chat + copier-coller)

*Ces galères venaient du workflow d'avant (scripts Python copiés-collés pour écrire les fichiers). Avec Claude Code qui édite directement, elles ne devraient plus se produire — mais leur esprit reste :*
- App.jsx a déjà été vidé par un script de remplacement mal ciblé → **toujours vérifier le résultat d'une grosse édition avant de continuer**, et commit avant (règle 3).
- Bugs Unicode (`\u00e9` interprétés) dans les scripts de remplacement → si un script Python doit générer du texte, préférer l'UTF-8 propre.
- "Sauvegardes fantômes" VS Code (Vite compilait une ancienne version) → si le comportement du site ne colle pas au code, vérifier le contenu réel du fichier sur le disque avant de débugger la logique.
