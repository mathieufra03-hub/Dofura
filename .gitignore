# Base de données
dofura.db
frontend/dofura.db

# JSON generes par un script mais pas source pour Railway (safe hors Git)
dofura_effets_speciaux.json
audit_sorts.json

# NOTE: dofura_monstres.json / dofura_sorts.json / dofura_effects.json restent
# suivis en Git : Railway n'a pas de volume persistant (chantier #1), donc
# le depot Git est leur seule source au deploiement. A revoir une fois le
# volume Railway en place.

# Backups locaux
backups/

# Python
__pycache__/
*.pyc
*.pyo

# Node
frontend/node_modules/
frontend/dist/
frontend/.vite/

# OS
.DS_Store
Thumbs.db