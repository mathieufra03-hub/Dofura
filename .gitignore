# Base de données
dofura.db
frontend/dofura.db

# JSON generes par un script mais pas lus par main.py (safe hors Git)
audit_sorts.json

# NOTE: dofura_monstres.json / dofura_sorts.json / dofura_effects.json /
# dofura_effets_speciaux.json restent suivis en Git : Railway n'a pas de
# volume persistant (chantier #1), donc le depot Git est leur seule source
# au deploiement. dofura_effets_speciaux.json est lu par main.py (chantier
# #3, piege #10). A revoir une fois le volume Railway en place.

# Fichiers bruts du scraping (500+ Mo, regenerables via scripts/scraper_items.py)
# JAMAIS commitables : GitHub refuse les fichiers > 100 Mo
dofura_*_brut.json

# Backups locaux
backups/

# Python
__pycache__/
*.pyc
*.pyo

# Node
frontend/node_modules/
frontend/dist/
frontend/.vite/

# OS
.DS_Store
Thumbs.db